export const Pagination = ({ state, className }) => {
    return (
        <div className={`w-6 flex flex-col items-center p-4 h-6 rounded-2xl justify-center relative ${className}`}>
            {/* ... */}
        </div>
    );
};